server:
  contextPath: /api/v1
#  port: 8080

spring:
  profiles:
    active: develop
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Tokyo

logging:
  file: logs/app.log
  level:
    ROOT: INFO

swasd:
  sample: test_with_override
